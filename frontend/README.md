# Careerak Frontend

Vite + React frontend for signup/login.

Setup:

1. Run `npm install` in `frontend`.
2. Start dev server with `npm run dev`.

The dev server proxies `/api` to `http://localhost:5000`.
